(O, P) => {
  // 1. Let desc be the result of calling the [[GetProperty]] internal method of O with property name P.
  let desc = O.GetProperty(O, P)
  // 2. If desc is undefined, then return false.
  if (= desc undefined) return false
  // 3. Else return true.
  else return true
}
